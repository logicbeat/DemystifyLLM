// Utility functions for markdown rendering
// This will be enhanced in Phase 3

export const sanitizeMarkdown = (content: string): string => {
  // TODO: Implement markdown sanitization in Phase 3
  // For now, just return the content as-is
  return content;
};

export const extractTitle = (_markdownContent: string): string | null => {
  // TODO: Implement title extraction from markdown in Phase 3
  // Look for first H1 heading
  return null;
};